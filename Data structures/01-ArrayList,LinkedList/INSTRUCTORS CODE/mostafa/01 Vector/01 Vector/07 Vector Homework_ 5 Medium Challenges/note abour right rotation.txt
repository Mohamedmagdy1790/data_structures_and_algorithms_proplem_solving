- If we are allowed to make copy of the array
- then we can just copy directly the shited array in O(n) instead total of O(n^2)

e.g. by Hesham Mohamed


void right_rotation2(int times)
{
    times=times%size;

    int *arr1=new int [size];

    for(int i=size-times,j=0;i<size;i++,j++)
        arr1[j]=arr[i];
        
    for(int i=0,j=times;i<size-times;i++,j++)
        arr1[j]=arr[i];
        
    swap(arr1,arr);
    delete arr1;
}
